export enum OrderStatus {
  placed = 'placed',
  shipped = 'shipped',
  onWay = 'onWay',
  delivered = 'delivered',
  canceled = 'canceld',
  refunded= 'refunded'
}

export enum PaymendMethod {
    card = 'card',
    cash = 'cash'
}
